console.log(window); // Window object is the global object in the browser

// DOM: Document Object Model
console.log(document); // Document object is the root object of the DOM tree
// DOM tree is the tree of all the HTML elements in the browser

console.log(document.body)

document.body.style.backgroundColor = "red";

// BOM: Browser Object Model
// Alert, Confirm, Prompt are the BOM objects

location.href = "https://www.google.com";